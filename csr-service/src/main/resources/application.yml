server:
  port: 5001
spring:
  main:
    banner-mode: off
  application:
    name: csr-service
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    initilaization-mode: always
    platform: postgres
    url: "jdbc:postgresql://localhost:5431/csr"
    username: "root"
    password: "root"
  security:
    user:
      password: "foo"
logging:
  level:
    org:
      springframework: ERROR